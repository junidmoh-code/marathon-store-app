{
  "name": "marathon-club-functions",
  "description": "Firebase Cloud Functions for Marathon Club",
  "engines": {
    "node": "22"
  },
  "main": "index.js",
  "scripts": {
    "deploy": "echo 'REFUSED: this project shares its Firebase functions with other apps, so a bare --only functions deploy would redeploy EVERY function including ones nobody reviewed. Deploy by name instead:  firebase deploy --only functions:<name>' && exit 1",
    "logs": "firebase functions:log",
    "test": "node --test"
  },
  "dependencies": {
    "@anthropic-ai/sdk": "^0.30.0",
    "@google-cloud/secret-manager": "^6.2.0",
    "cors": "^2.8.6",
    "firebase-admin": "^12.0.0",
    "firebase-functions": "^6.0.0",
    "google-auth-library": "^9.15.1",
    "openai": "^6.45.0",
    "pdfjs-dist": "^5.4.296",
    "sharp": "^0.33.5"
  },
  "private": true
}
