{
  "name": "marathon-card-recon-poller",
  "private": true,
  "type": "module",
  "description": "IMAP dependencies for the card recon email poller. Kept OUT of functions/package.json on purpose: those dependencies are installed into every Cloud Function deploy, and a mail client has no business in the production functions runtime. firebase-admin is borrowed from functions/ rather than duplicated here.",
  "dependencies": {
    "imapflow": "^1.0.191",
    "mailparser": "^3.7.2"
  }
}
